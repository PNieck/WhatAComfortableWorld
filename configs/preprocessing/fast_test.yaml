general:
  seed: 42

preprocessing:
  test_size: 0.1
  validate_size: 0.1
  max_floor_plans: 1000
  output: "./data/fast_test"
  data_augmentation:
    - permutation:
        percentage: 0.4
    - symmetry:
        percentage: 0.4
    - rotation:
        percentage: 0.4